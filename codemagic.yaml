workflows:
  android-workflow:
    name: BotsGuied Android Build
    environment:
      flutter: stable
      groups:
        - default
    scripts:
      - name: Install dependencies
        script: |
          flutter pub get
      - name: Build release APK
        script: |
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/
